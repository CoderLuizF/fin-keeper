function Sidebar() {
  return (
    <aside className="sidebar">
      <h2 className="sidebar-title">FinKeeper</h2>

      <nav className="sidebar-nav">
        <a href="#">Dashboard</a>
        <a href="#">Expenses</a>
        <a href="#">Reports</a>
        <a href="#">Settings</a>
      </nav>
    </aside>
  );
}

export default Sidebar;
